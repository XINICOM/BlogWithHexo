<div
    class="aside-resize-top mt-16 lg:mt-20 border-b color-border color-text-content text-center"
>
    <div class="callout caution">
        <div class="max-w-fit mx-auto">
            <div class="callout-title">这是 category-index.ejs</div>
            <p>
                鉴于本网站还未完工，一般本页面不应该使用 category-index.ejs
                来生产。
            </p>
            <% page.categoryTopNodes.forEach(c => { %>
            <p><%= c.name %></p>
            <% }) %>
        </div>
    </div>
</div>
